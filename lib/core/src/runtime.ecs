namespace core {
    fn Print(message: string);
    fn print(message: string);
    fn Stop();
    fn stop();
    fn spawn(): entity;
    fn getFirstEntity(): entity;

    system Time();
    system Window();
    system Renderer();
}
